<div>
  {{#if this.albums.items}}
    {{#each this.albums.items}}
      <div>
        <h4>{{name}}</h4>
        <img src="{{images.[0].url}}" alt="{{name}}">
        <a href="/albums/{{id}}">
        <button>View Tracks</button>
        </a>
      </div>
    {{/each}}
  {{/if}}
</div>