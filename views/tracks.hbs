<div>
    {{#each this.tracks.items}}
      <div>
        <h4>{{name}}</h4>
        <audio controls src="{{preview_url}}">
          <a href="{{preview_url}}"></a>
        </audio>
      </div>
    {{/each}}
</div>